<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.RECORD_AUDIO"/> <!-- Audio Recording -->
    <uses-permission android:name="android.permission.CAMERA"/> <!-- Video Recording -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/> <!-- Modify Audio Settings -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/> <!-- For Bluetooth Headset (Android 12+) -->
    <uses-permission android:name="android.permission.CAPTURE_AUDIO_OUTPUT"/> <!-- Capture audio output -->
    <uses-permission android:name="android.permission.VIBRATE"/> <!-- Vibration during calls -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/> <!-- For foreground audio/video service -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/> <!-- Keep the device awake during calls -->

    <!-- For Managing Calls -->
    <uses-permission android:name="android.permission.CALL_PHONE"/> <!-- Initiate outgoing calls -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/> <!-- Read phone state during calls -->
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS"/> <!-- Auto answer calls (Android 8.0+) -->

    <!-- Android 13 (API 33+) Specific Permissions -->
    <uses-permission android:name="android.permission.RECORD_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/> <!-- Read image files -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/> <!-- Read video files -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/> <!-- Post notifications -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/> <!-- Manage files and recordings -->

    <!-- Network Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:label="videosdk_flutter_example"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
